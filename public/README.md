# 靜態檔案

放在這個資料夾的檔案會以網站根目錄提供，例如 `public/logo.png` 對應網址 `/logo.png`。

## LOGO

把公司 LOGO 命名為 `logo.png`（或 `logo.svg`）上傳到這個資料夾，
再告知即可接上側邊欄品牌區。

建議使用**只有黃色葉形圖案、不含公司名稱文字**的版本，
因為側邊欄寬度只有 200px，含文字的完整版縮到 32px 會看不清楚。
